# Optional: enable cloud AI tutor/generation (zero-retention). Without these, the app
# uses fully on-device heuristics — still fully functional.
# LLM_API_KEY=
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_MODEL=gpt-4o-mini
